{
  "name": "yoldr",
  "version": "1.0.0",
  "description": "YOLDR — Principal-Protected DeFi Vault with Leveraged Yield Positions",
  "scripts": {
    "compile": "npx hardhat compile && node scripts/copyAbis.js",
    "copy-abis": "node scripts/copyAbis.js",
    "test": "npx hardhat test",
    "deploy:arbitrum": "npx hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "mint": "npx hardhat run scripts/mintTestTokens.ts --network arbitrumSepolia",
    "deploy:local": "npx hardhat run scripts/deploy.ts --network localhost"
  },
  "keywords": [
    "defi",
    "vault",
    "yield",
    "iexec",
    "nox",
    "arbitrum"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@iexec-nox/nox-confidential-contracts": "^0.1.0",
    "@iexec-nox/nox-protocol-contracts": "^0.2.2",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.0"
  }
}
